% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot-helpers.R
\name{as_chunk_factor}
\alias{as_chunk_factor}
\title{Convert a time_chunk vector to an ordered factor}
\usage{
as_chunk_factor(x, labels = "code")
}
\arguments{
\item{x}{A \code{time_chunk} vector.}

\item{labels}{Character scalar. Controls the level labels. One of:
\itemize{
\item \code{"code"} (default) — \code{"fa26"}, \code{"sp27"}, ...
\item \code{"name"} — \code{"Fall 2026"}, \code{"Spring 2027"}, ...
\item \code{"ay"} — \code{"2026-27"}, \code{"2026-27"}, ... (may repeat)
\item \code{"key"} — sortable composite key
\item \code{"iso_date"} — start date string
\item Any glue template string (e.g. \code{"{name} ({ay})"})
}}
}
\value{
An ordered factor of the same length as \code{x}, with levels in
chronological order.
}
\description{
Creates an ordered factor suitable for use with \code{ggplot2} discrete scales.
Levels are ordered chronologically by start date, so ggplot2 will display
periods in the correct temporal order.

For continuous scales, use \code{\link[=start_date]{start_date()}}, \code{\link[=end_date]{end_date()}}, or \code{\link[=mid_date]{mid_date()}}
directly with \code{scale_x_date()}. See the ggplot2 vignette for examples.
}
\examples{
use_chunk_preset("us_semester")
x <- time_chunk(c("su27", "fa26", "sp27"))

as_chunk_factor(x, labels = "code")
# ordered factor: fa26 < sp27 < su27

as_chunk_factor(x, labels = "name")
# ordered factor: Fall 2026 < Spring 2027 < Summer 2027
}
