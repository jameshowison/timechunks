% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calendar.R
\name{set_chunk_calendar}
\alias{set_chunk_calendar}
\title{Set the active calendar configuration}
\usage{
set_chunk_calendar(
  periods,
  year_start_period,
  yyyym_strict = FALSE,
  yyyym_map = NULL,
  name = NULL
)
}
\arguments{
\item{periods}{A list of period definitions. Each element must be a named
list with fields \code{name} (character), \code{code} (character), and
\code{start_mmdd} (character, format \code{"MM-DD"}). Optionally includes
\code{end_mmdd} to override the computed end date.}

\item{year_start_period}{Character. The \code{name} of the period that begins
the academic or fiscal year (e.g. \code{"Fall"}).}

\item{yyyym_strict}{Logical. If \code{TRUE}, \code{\link[=time_chunk]{time_chunk()}} errors on YYYYM
inputs where the month maps ambiguously to multiple periods. Default \code{FALSE}.}

\item{yyyym_map}{Optional named list mapping two-digit month strings to
period names, e.g. \code{list("08" = "Fall", "01" = "Spring")}. Overrides
automatic month-to-period resolution.}

\item{name}{Optional character string naming this calendar (used in printing).}
}
\value{
Invisibly returns the calendar configuration list.
}
\description{
Configures the global calendar used by \code{\link[=time_chunk]{time_chunk()}} and related functions.
The configuration is stored in \code{options(timechunks.calendar = ...)} and
persists for the duration of the R session.
}
\examples{
set_chunk_calendar(
  periods = list(
    list(name = "Fall",   code = "fa", start_mmdd = "08-23"),
    list(name = "Spring", code = "sp", start_mmdd = "01-15"),
    list(name = "Summer", code = "su", start_mmdd = "06-01")
  ),
  year_start_period = "Fall"
)
}
