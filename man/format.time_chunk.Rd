% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{format.time_chunk}
\alias{format.time_chunk}
\title{Format time_chunk vectors as strings}
\usage{
\method{format}{time_chunk}(x, style = "name", ...)
}
\arguments{
\item{x}{A \code{time_chunk} vector.}

\item{style}{Character scalar. A named style or a glue template string.
Default \code{"name"}.}

\item{...}{Ignored (required by S3 generic).}
}
\value{
A character vector of the same length as \code{x}.
}
\description{
Formats a \code{time_chunk} vector using one of the built-in styles or a
glue-style template string.

\strong{Named styles:}
\itemize{
\item \code{"code"} — \code{"fa26"}
\item \code{"name"} — \code{"Fall 2026"}
\item \code{"ay"} — \code{"2026-27"}
\item \code{"key"} — \code{"2026-27_1_08_Fall"} (sortable composite, round-trips via parsing)
\item \code{"iso_date"} — \code{"2026-08-23"} (start date)
}

\strong{Glue templates} (any string containing \verb{\{}):
\itemize{
\item Variables available: \code{name}, \code{code}, \code{year}, \code{ay}, \code{chunk_index},
\code{start_date}, \code{end_date}, \code{mid_date}.
\item Example: \code{"{ay}_{chunk_index}_{name}"} → \code{"2026-27_1_Fall"}
}
}
\examples{
use_chunk_preset("us_semester")
x <- time_chunk("fa26")

format(x, style = "code")      # "fa26"
format(x, style = "name")      # "Fall 2026"
format(x, style = "ay")        # "2026-27"
format(x, style = "key")       # "2026-27_1_08_Fall"
format(x, style = "iso_date")  # "2026-08-23"
format(x, style = "{ay}_{chunk_index}_{name}")  # "2026-27_1_Fall"
}
